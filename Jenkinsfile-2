pipeline {
	agent {
		docker {
			image 'golang:alpine'
		}
	}

	options {
		timestamps()
		skipStagesAfterUnstable()
	}

	stages {
		stage('Build') {
			steps {
				echo "Hello World 3"
				script {
					currentBuild.result = 'UNSTABLE'
				}
			}
		}

		stage('Deploy') {
			steps  {
				echo "deploy running..."
			}
		}
	}
}
