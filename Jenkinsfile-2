pipeline {
	agent {
		docker {
			image 'ubuntu:latest'
		}
	}

	triggers {
		pollSCM('* * * * *')
	}

	options {
		timestamps()
	}

	stages {
		stage('Build') {
			steps {
				checkout([$class: 'GitSCM',
					branches: [[name: "origin/dev"]],
					userRemoteConfigs: [[
						url: 'https://github.com/hsuryalim/test.git'
					]]
				])
			}
		}
	}
}
