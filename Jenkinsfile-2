pipeline {
	agent {
		docker {
			image 'maven:3.5.3-jdk-10-slim'
			args '-e someEnv=dev'
		}
	}

	options {
		timestamps()
	}

	stages {
		stage('Build') {
			steps {

				sh "echo $someEnv"
			}
		}
	}
}
