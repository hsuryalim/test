pipeline {
	agent any
	
	options {
		timestamps()
	}

	environment {
		version="1.0.0"
	}

	stages {
		stage('Build') {
			when {
				expression {
					version == "1.0.0"
				}
			}

			steps {
				echo "${version}"
			}
		}
	}
}
