pipeline {
	agent {
		docker {
			image 'golang:alpine'
		}
	}

	options {
		timestamps()
		overrideIndexTriggers(true)
	}

	stages {
		stage('Build') {
			steps {
				echo "Hello World 3"
			}
		}
	}
}
