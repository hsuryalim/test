pipeline {
	agent any

	options {
		timestamps()
		skipDefaultCheckout()
	}

	stages {
		stage('Hello World') {
			steps {
				echo "Before error"
				sleep 2
			}
		}
		stage('Test') {
			steps {
				echo "test"
			}
		}
	}
}
