pipeline {
	agent any
	
	options {
		timestamps()
	}

	environment {
		some_name="jeff"
	}

	stages {
		stage('Build') {
			when {
				equals expected: "jeff", actual: some=name
			}

			steps {
				echo "${some_name}"
			}
		}
	}
}
