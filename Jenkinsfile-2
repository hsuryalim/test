pipeline {
	agent {
		docker {
			image 'ubuntu:latest'
		}
	}

	triggers {
		upstream(upstreamProjects: 'job-1','job-2', 
		threshold: hudson.model.Result.SUCCESS)
	}

	options {
		timestamps()
	}

	stages {
		stage('Build') {
			steps {
				checkout([$class: 'GitSCM',
					branches: [[name: "origin/dev"]],
					userRemoteConfigs: [[
						url: 'https://github.com/hsuryalim/test.git'
					]]
				])
			}
		}
	}
}
