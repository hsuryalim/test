pipeline {
	agent {
		docker {
			image 'ubuntu:latest'
		}
	}

	options {
		timestamps()
	}

	parameters {
		choice(choices:['qa','dev'], description: 'Please choose environment', name: 'ENV')
	}

	stages {
		stage('Build') {
			steps {
				echo "${ENV}"
			}
		}
	}
}
