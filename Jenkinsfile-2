pipeline {
	agent {
		docker {
			image 'ubuntu:latest'
			args '-e someEnv=dev'
		}
	}

	options {
		timestamps()
	}

	stages {
		stage('Build') {
			steps {
				checkout([$class: 'GitSCM',
					branches: [[name: "origin/dev"]],
					userRemoteConfigs: [[
						url: 'https://github.com/hsuryalim/test.git'
					]]
				])

				sh "echo $someEnv"
			}
		}
	}
}
