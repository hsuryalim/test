pipeline {
	agent {
		docker {
			image 'golang'
		}
	}
	stages {
		stage('Hello World') {
			options {
				timeout(time:1, unit: 'SECONDS')
			}
			steps {
				echo "Before error"
				sleep 2
			}
		}
	}
}
