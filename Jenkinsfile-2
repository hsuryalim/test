pipeline {
	agent {
		docker {
			image 'golang'
		}
	}
	stages {
		stage('Hello World') {
			options {
				retry(3)
			}
			steps {
				echo "Before error"
				script {
					currentBuild.status = "FAILURE"
				}
				echo "After error"
			}
		}
	}
}
