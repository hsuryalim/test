pipeline {
	agent {
		docker {
			image 'ubuntu:latest'
			args '-e someEnv=dev'
			alwaysPull true
			customWorkspace '/var/lib/jenkins/workspace/customWorkspace'
		}
	}

	options {
		timestamps()
	}

	stages {
		stage('Build') {
			steps {

				sh " printenv someEnv"
			}
		}
	}
}
