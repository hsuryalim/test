pipeline {
	agent any
	
	options {
		timestamps()
	}

	environment {
		DEPLOY_TO="production"
	}

	stages {
		stage('Build') {
			when {
				environment name: 'DEPLOY_TO', value:'production'
			}

			steps {
				echo "Deployed"
			}
		}
	}
}
