pipeline {
	agent {
		docker {
			image 'golang:alpine'
		}
	}

	options {
		timestamps()
		overrideIndexTriggers(false)
	}

	stages {
		stage('Build') {
			steps {
				echo "Hello World 3"
			}
		}
	}
}
