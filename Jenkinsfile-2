pipeline {
	agent {
		docker {
			image 'ubuntu:latest'
		}
	}

	triggers {
		// minute, hour, day of month , month, day of week
		cron('H */2 1-3 1,3 2-4')
	}

	options {
		timestamps()
	}

	stages {
		stage('Build') {
			steps {
				echo "running.."
			}
		}
	}
}
