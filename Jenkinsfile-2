pipeline {
	agent {
		docker {
			image 'ubuntu:latest'
			args '-e someEnv=dev'
			alwaysPull true
		}
	}

	options {
		timestamps()
	}

	stages {
		stage('Build') {
			steps {

				sh " printenv someEnv"
			}
		}
	}
}
