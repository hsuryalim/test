pipeline {
	agent {
		docker {
			image 'golang:alpine'
		}
	}

	options {
		timestamps()
		disableConcurrentBuilds()
	}

	stages {
		stage('Build') {
			steps {
				sleep(time: 10, unit: 'SECONDS')
				echo "building..."
			}
		}
	}
}
