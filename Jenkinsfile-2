pipeline {
	agent {
		docker {
			image 'golang:alpine'
		}
	}

	options {
		timestamps()
		checkoutToSubdirectory('working-drectory')
	}

	stages {
		stage('Build') {
			steps {
				echo "Hello World 3"
			}
		}

		stage('Deploy') {
			steps  {
				echo "deploy running..."
			}
		}
	}
}
